{
  "name": "s0_smoke_subscription",
  "description": "SMOKE TEST via Claude Pro/Max sign-in \u2014 no API key. Drives the `claude` CLI. Deliberately tiny (2 agents, $10 increment): each decision takes seconds and draws on your plan allowance. Run `claude` once and sign in first.",
  "roster": "mbti",
  "agents": [
    "estp",
    "intj"
  ],
  "seed": 7,
  "repeats": 1,
  "turn_order": "rotate",
  "rules": {
    "item_value": 100.0,
    "increment": 10.0,
    "default_budget": 250.0,
    "max_rounds": 8,
    "max_bid_multiple": 2.0
  },
  "memory": {
    "within_game": true,
    "cross_game": false,
    "mode": "transcript",
    "keep_rounds": 8
  },
  "overrides": {
    "model": {
      "provider": "cli",
      "command": "claude",
      "max_tokens": 1024,
      "timeout": 180
    }
  }
}
