# Copy to .env (chmod 600) and fill in only the providers you intend to use.
# Load with:  set -a && . ./.env && set +a
# .env is gitignored. Never commit a real key.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
